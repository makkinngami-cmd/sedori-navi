# 2-1. 買取価格収集・チャート表示 HANDOFF

## 最新引き継ぎ 2026-06-29

### 現在地

- 日次スクレイピング、JAN統合、GitHub Pages向け `docs/` 反映は稼働中。
- 主運用は Windows タスクスケジューラの `run_scraper.ps1`（毎日12:17）。
- 価格アラーム `docs/alerts.html` は本番公開済み。7日以内の価格変動のみ表示、カテゴリフィルター・お気に入りフィルター付き。
- ヤフオク低額混入対策として `scrape_yahoo.py` にPython側 `min_price` フィルタを追加済み。PSVR2・PS5ソフト等のPRICE_FLOORSも設定済み。
- チャート横軸に期間切り替えタブ（1ヶ月/3ヶ月/6ヶ月/全期間）を実装済み。デフォルト全期間、localStorage保存。
- 買取強化中リサーチの仕組みを実装済み。`generate_ichome_boost_candidates.py` が `/api/index/getImpoProduct` から強化中リストを取得し、未登録分を優先度S/A/B/C付きで `reports/ichome_boost_candidates.*` に出力。`run_scraper.ps1` で週1（月曜）自動更新。商品追加はレポートを見てユーザーが選び、Claudeが3ファイル（products.py / index.html / alerts.html のPRODUCT_CATEGORIES）＋msrp.csvへ追記するバッチ方式。
- 2026-06-29: 強化中Sランクから16商品を追加。iPhone 17無印 256/512GB各5色（定価129,800/164,800）、FUJIFILM X-T50 ボディ黒銀・X-E4 ボディ黒銀/レンズキット黒銀（market）。強制スクレイプで16件すべて当日取得確認済み。方針はiPhoneは17/17 Proのみ（16系・17eは追加しない）。
- カメラ追加20件のうち買取一丁目のみだった8件は、X-M5シルバーのみヤフオク落札データが出現（¥149,000）。他7件は変化なし。
- 取得業者0店は `トモダチコレクション Switch 2`（未発売）の1件のみ。1店のみ33件は大半がカメラ新規追加分で想定内。

### 次にやること

1. 価格アラーム: 実運用でしばらく様子を見る。条件や表示に改善要望があれば対応 ／ 随時
2. カメラ候補: 買取一丁目のみ7件はウォッチ継続（X-M5シルバーはヤフオク出現済み） ／ 次回以降
3. 商品追加: 取得業者数0/1の改善または未整理分野から優先度を再確認する ／ 次回以降
4. 買取強化中: 週1レポート `reports/ichome_boost_candidates.md` を見て、追加する強化商品をユーザーが選ぶ。Sランク一部追加済み。残りSランク（Pixel等）やAランク、X-T50レンズキット派生の追加を継続検討 ／ 次回以降
5. 競合対応（買取サーチ比較を受けて）: 競合未導入店（海峡通信・モバイルミックス等）を2〜3店だけ評価し、既存商品のカバレッジ補強として追加検討する ／ ユーザー承認後
6. 競合対応: まとめ売り試算は単体実装せず、在庫管理（2-2）と統合する案として保留 ／ 2-2着手時に再検討

（競合比較の詳細は WORKLOG.md 2026-06-28 を参照。自社の核は「ヤフオク相場×履歴チャート×買取率」。機能数では張り合わない方針。）

### 未解決

- 今後追加される新規ゲームソフトは、公式ページなどで確認してからMSRP追加する。
- 価格アラームの対象は買取一丁目のみ。複数業者横断は未実装。
- ヤフオク低額混入はPRICE_FLOORSで対処中。新商品で混入が見つかったら同様に追加する。

### Claude Code向け結論

- フェーズ1商品追加・フェーズ2価格アラームとも完了。運用フェーズ。
- 確認用スクリーンショット類（`reports/alerts_*.png` / `reports/price_alert_*.png`）は未コミット。削除する場合はユーザー確認が必要。

### 触ってはいけないもの

- `data/raw/` は削除しない。
- `.bak_...` ファイルは削除しない。
- 候補一覧 `reports/ichome_product_candidates.*` / `reports/ichome_game_recommendations.*` は捨てない。
- データ未取得時にCSVを手で埋めて終わらせない。仕組み側を確認する。
- 親フォルダ直下MDへ細かい作業ログを書き足さない。履歴は `WORKLOG.md` に書く。
